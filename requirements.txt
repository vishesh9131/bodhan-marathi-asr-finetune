# Pinned after env build (see outputs/env_build.log). Install into a fresh env:
#   conda create -y -n bodhan-asr python=3.10 pip && conda activate bodhan-asr
#   pip install -r requirements.txt
nemo_toolkit[asr]
huggingface_hub
librosa
jiwer
soundfile
datasets
